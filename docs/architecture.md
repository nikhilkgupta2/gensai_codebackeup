# Architecture

The application is split into a React frontend and FastAPI backend. PostgreSQL is the primary data store. Backend modules are organized by API routes, services, repositories, schemas, models, and infrastructure concerns.

Tenant-aware domain models and scoped repository methods will be introduced in later phases.
